server:
  port: 9092

spring:
  profiles:
    active: dev
  application:
    name: FOOD-CATALOG-SERVICE
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update